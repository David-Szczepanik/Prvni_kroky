#include <SDL2/SLD.h>
#include <iostream>
#include <random>
#include <ranges>
#include <algorithm>

int main() {

  std::random_device rd;
  std::uniform_int_distribution d(1,99);
  std::vector<int> v;

  // populate our vector with rnd numbers
  //
  for(unsigned int i = 0; i < v.size();i++)
  {
    for(unsigned int j = i; j < v.size); j++)
  }
}